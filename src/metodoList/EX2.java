package metodoList;

import java.util.ArrayList;

/*public class EX2 {
    public static void main(String[] args) {
        List<Double> temperaturas = new ArrayList<>();
        Scanner scanner = new Scanner(System.in);

        // Coleta de temperaturas
        for (int i = 0; i < 6; i++) {
            System.out.println("Digite a temperatura média do mês " + (i + 1) + ": ");
            double temp = scanner.nextDouble();
            temperaturas.add(temp);
        }

        // Calculando a média das temperaturas
        double soma = 0;
        for (double temp : temperaturas) {
            soma += temp;
        }
        double media = soma / temperaturas.size();
        System.out.println("\nMédia semestral de temperatura: " + media);

        // Lista com os meses correspondentes
        String[] meses = {"Janeiro", "Fevereiro", "Março", "Abril", "Maio", "Junho"};

        // Exibindo temperaturas acima da média
        System.out.println("\nTemperaturas acima da média:");
        for (int i = 0; i < temperaturas.size(); i++) {
            if (temperaturas.get(i) > media) {
                System.out.println(meses[i] + ": " + temperaturas.get(i) + "°C");
            }
        }

        // Fechando Scanner
        scanner.close();
    }
}*/

//Refazendo o script 
public class EX2 {
     public static void main(String[] args) {
        List<Double> grausC = new ArrayList<>(); 
        Scanner scanner = new Scanner(System.in);


     }


}
